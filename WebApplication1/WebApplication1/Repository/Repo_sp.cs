using WebApplication1.Data;

namespace WebApplication1.Repository
{
    public class Repo_sp
    {
        //private readonly GdttContext _context;
        //public Repo_sp(GdttContext context)
        //{
        //    _context = context;
        //}
        //public IQueryable<DmSpham> GetAll_sp()
        //{
        //    var query = _context.DmSpham.Select(x => new
        //    {
        //        TenSp = x.TenSp,
        //        MaSp = x.MaSp
        //    }).ToList();

        //    return query;
        //}
    }
}
// Because in GDTT database, there is no dm_sanpham table so I comment these lines here. 
// If using, please modify the above code to be compatible